# BenchWeave — project instructions

## nanoDLA logic analyser: capture → decode → annotate

Use the `nanodla-decode` skill whenever capturing or decoding with the nanoDLA
(`benchweave-nanodla` MCP server). Never leave a capture without a note in its
manifest. Gotchas: `docs/nanodla-mcp-server.md`.
